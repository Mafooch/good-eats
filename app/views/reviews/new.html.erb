<h3>Add a review for <%= @restaurant.name %></h3>

<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <br><br>
  <%= f.submit "Submit review" %>
<% end %>
